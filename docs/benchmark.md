# Benchmark Workflow

The canonical benchmark reference is now
[`docs/system/BENCHMARK.md`](system/BENCHMARK.md).

This compatibility pointer exists so older links to `docs/benchmark.md` keep
working.
